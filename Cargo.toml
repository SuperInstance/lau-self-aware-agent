[package]
name = "lau-self-aware-agent"
version = "0.1.0"
edition = "2021"
license = "MIT"
repository = "https://github.com/SuperInstance/lau-self-aware-agent"
description = "Limited models studying themselves — capability curvature, blind spots, exceeds-design detection, and the recursive self-study fixed point"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
nalgebra = "0.33"

[dev-dependencies]
